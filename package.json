{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "bash scripts/dev-stagewire.sh",
    "dev:web": "PORT=${PORT:-5173} BASE_PATH=${BASE_PATH:-/} API_PORT=${API_PORT:-5174} pnpm --dir artifacts/stagewire dev",
    "dev:api": "PORT=${API_PORT:-5174} pnpm --dir artifacts/api-server dev",
    "db:prepare": "pnpm --dir lib/db push",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "audit:stagewire-mobile": "node scripts/stagewire-mobile-audit.mjs"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
